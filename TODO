1. Fail if http rpc endpoint fails
2. Take first spec version from the runtime instead of writing it in the config file
3. If no finalized blocks are found in rocksdb, fail or go in live mode
4. Batch update extrinsics on transaction in events postgres client